name: Set FP labels

on:
  issues:
    types: [labeled]

jobs:
  add-confirmed-label:
    runs-on: ubuntu-latest
    steps:
      - name: add 'Approved'
        env: 
          PAYLOAD: ${{ github.event }}
          LABEL_NAME: ${{ github.event.label.name }}
        run: echo "woot"
